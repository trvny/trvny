name: Compile gh-aw sources

on:
  pull_request:
    paths:
      - .github/workflows/docs-orchestrator.md
      - .github/workflows/docs-worker.md
      - .github/workflows/compile-gh-aw.yml

permissions:
  contents: write

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Install gh-aw
        id: setup
        uses: github/gh-aw/actions/setup-cli@80a44d777dd097c7f4455162764590f66937d6f6
        with:
          version: v0.87.0

      - name: Compile
        id: compile
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -o pipefail
          gh aw compile docs-orchestrator docs-worker --strict --approve 2>&1 | tee /tmp/gh-aw-compile.log

      - name: Stage generated files
        if: always()
        env:
          SETUP_OUTCOME: ${{ steps.setup.outcome }}
          COMPILE_OUTCOME: ${{ steps.compile.outcome }}
          CROSS_REPO_TOKEN_PRESENT: ${{ secrets.GH_AW_GITHUB_TOKEN != '' }}
        run: |
          mkdir -p .github/aw/staged
          {
            echo "setup=${SETUP_OUTCOME}"
            echo "compile=${COMPILE_OUTCOME}"
            echo "cross_repo_token=${CROSS_REPO_TOKEN_PRESENT}"
            echo
            cat /tmp/gh-aw-compile.log 2>/dev/null || true
          } > .github/aw/compile-status.txt

          if [ "$COMPILE_OUTCOME" = "success" ]; then
            cp .github/workflows/docs-orchestrator.lock.yml .github/aw/staged/docs-orchestrator.lock.yml
            cp .github/workflows/docs-worker.lock.yml .github/aw/staged/docs-worker.lock.yml
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/aw
          git commit -m "ci: stage central docs compilation" || exit 0
          git push origin HEAD:${{ github.event.pull_request.head.ref }}

      - name: Upload generated locks
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: central-docs-gh-aw
          include-hidden-files: true
          retention-days: 1
          path: |
            .github/aw/staged/docs-orchestrator.lock.yml
            .github/aw/staged/docs-worker.lock.yml
            .github/aw/actions-lock.json
            .github/aw/compile-status.txt

      - name: Fail on compile error
        if: ${{ steps.compile.outcome != 'success' }}
        run: exit 1
