name: Quarto

on: # yamllint disable-line rule:truthy
  pull_request:
    paths:
      - "token-worldcup/**"
      - ".github/workflows/quarto.yml"
  push:
    branches: [main]
    paths:
      - "token-worldcup/**"
      - ".github/workflows/quarto.yml"
  schedule:
    - cron: "17 4 * * 1"
  workflow_dispatch:
    inputs:
      task:
        description: Quarto task
        required: true
        default: check
        type: choice
        options:
          - check
          - update
          - all

permissions:
  contents: read

jobs:
  check:
    if: >-
      github.event_name == 'pull_request' ||
      github.event_name == 'push' ||
      (github.event_name == 'workflow_dispatch' &&
       (inputs.task == 'check' || inputs.task == 'all'))
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Install tokenizer tools
        run: npm ci --prefix token-worldcup --ignore-scripts
      - name: Verify OpenAI token counts
        run: npm run check:openai --prefix token-worldcup
      - uses: quarto-dev/quarto-actions/setup@v2
        with:
          version: 1.10.18
      - name: Render token worldcup
        uses: quarto-dev/quarto-actions/render@v2
        with:
          path: token-worldcup/token-worldcup.qmd
      - name: Render effort matrix
        uses: quarto-dev/quarto-actions/render@v2
        with:
          path: token-worldcup/effort-matrix.qmd
      - name: Upload manually rendered HTML
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v7
        with:
          name: token-worldcup-html-${{ github.sha }}
          path: |
            token-worldcup/token-worldcup.html
            token-worldcup/effort-matrix.html
          if-no-files-found: error
          retention-days: 7
      - name: Verify committed HTML is current
        if: github.event_name != 'workflow_dispatch'
        run: git diff --exit-code -- token-worldcup

  update:
    if: >-
      github.event_name == 'schedule' ||
      (github.event_name == 'workflow_dispatch' &&
       (inputs.task == 'update' || inputs.task == 'all'))
    runs-on: ubuntu-slim
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v7
        with:
          token: ${{ secrets.GH_PAT }}
      - name: Setup Quarto
        uses: quarto-dev/quarto-actions/setup@v2
        with:
          version: "1.10.18"
      - name: Update extensions
        uses: mcanouil/quarto-extensions-updater@v2
        with:
          github-token: ${{ secrets.GH_PAT }}
          scan-directories: token-worldcup
          update-strategy: minor
