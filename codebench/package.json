{
  "name": "codebench",
  "private": true,
  "version": "1.0.0",
  "description": "QR & Barcode Studio — static, client-side.",
  "scripts": {
    "build": "node scripts/vendor.mjs",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "qr-code-styling": "1.9.2",
    "bwip-js": "4.11.2",
    "zxing-wasm": "3.1.2"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
