{
  "name": "docbench",
  "private": true,
  "version": "0.2.0",
  "description": "Local-first document and PDF toolbox.",
  "scripts": {
    "build": "node scripts/vendor.mjs && node scripts/portable.mjs",
    "typecheck": "npx --yes wrangler@4 types && npx --yes --package=typescript@7.0.2 tsc --noEmit",
    "check": "npm run build && npm run typecheck && node --check public/app.js && node scripts/check.mjs && node tests/pdf-core.test.mjs && npx --yes wrangler@4 deploy --dry-run",
    "dev": "npm run build && npx --yes wrangler@4 dev",
    "deploy": "npm run build && npx --yes wrangler@4 deploy"
  },
  "dependencies": {
    "@cantoo/pdf-lib": "2.9.1",
    "js-yaml": "5.3.0",
    "pdfjs-dist": "6.2.108",
    "qpdf-run": "0.2.1"
  },
  "overrides": {
    "pako": "^2.2.0"
  }
}
