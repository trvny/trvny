{
  "name": "kanarek-companion",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Cloudflare Worker runtime for Kanarek Companion, GPTomek, Gremlin Operator and specialist intelligence.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "node scripts/deploy.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "smoke:live": "node scripts/smoke-live.ts",
    "check": "npm run typecheck && npm test && node --check scripts/deploy.ts && node --check scripts/smoke-live.ts && wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260922.1",
    "typescript": "7.0.2",
    "wrangler": "4.136.2"
  },
  "overrides": {
    "sharp": "0.35.3"
  },
  "exports": {
    "./gremlin-core": "./src/gremlin-router.ts",
    "./github-app": "./src/github-app.ts"
  }
}
