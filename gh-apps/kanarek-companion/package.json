{
  "name": "kanarek-companion",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub App webhook endpoint for Kanarek companion.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "smoke:live": "node scripts/smoke-live.ts",
    "check": "npm run typecheck && npm test && node --check scripts/smoke-live.ts && wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260814.1",
    "typescript": "7.0.2",
    "wrangler": "4.123.0"
  },
  "overrides": {
    "sharp": "0.35.3"
  }
}
