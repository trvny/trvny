{
  "name": "kanarek-review",
  "version": "0.1.0",
  "private": true,
  "description": "Internal free-review provider Worker for Kanarek Companion.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "check": "npm run typecheck && npm test && wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260904.1",
    "typescript": "7.0.2",
    "wrangler": "4.129.0"
  },
  "overrides": {
    "sharp": "0.35.3"
  }
}
