{
  "name": "kanarek-companion",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub App webhook endpoint for Kanarek companion.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260731.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.116.0"
  },
  "overrides": {
    "sharp": "0.35.3"
  }
}
