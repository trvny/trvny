{
  "name": "status-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "One MCP tool that health-checks four travny projects (tvpi, Feedseek, weather, autka). Cloudflare Worker, free tier.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:icon": "node scripts/build-icon.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run typecheck && npm test && wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260922.1",
    "typescript": "7.0.2",
    "wrangler": "4.136.2"
  },
  "overrides": {
    "undici": "7.29.0"
  }
}
