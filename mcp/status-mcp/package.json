{
  "name": "status-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "One MCP tool that health-checks four travny projects (tvpi, feeds, weather, autka). Cloudflare Worker, free tier.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260804.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.118.0"
  },
  "overrides": {
    "sharp": "0.35.3",
    "undici": "7.29.0"
  }
}
