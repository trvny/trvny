{
  "name": "status-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "One MCP tool that health-checks all three travino projects (tvpi, feeds, autka). Cloudflare Worker, free tier.",
  "type": "module",
  "scripts": { "dev": "wrangler dev", "deploy": "wrangler deploy", "typecheck": "tsc --noEmit" },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.114.0"
  }
}
