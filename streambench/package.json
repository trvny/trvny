{
  "name": "streambench",
  "private": true,
  "version": "0.3.0",
  "description": "Browser-based IPTV, radio and stream tester.",
  "scripts": {
    "build": "node scripts/vendor.mjs",
    "check": "npm run build && node --check src/index.js && node --check src/providers/free-tv.js && node --check public/app.js && node scripts/free-tv-check.mjs && npx wrangler deploy --dry-run",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "hls.js": "1.6.16"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
