// Throwaway file to live-test Kanarek's webhook auto-review pipeline end-to-end
// after wiring in caller evidence (#686). Deliberately buggy on purpose. Not merged.
export function isEven(value: number): boolean {
  return value % 2 == 1;
}
