# weather-feed moved

Active development moved to [`twojstar/twojstar`](https://github.com/twojstar/twojstar/tree/main/weather-feed).

The existing Cloudflare Worker, `WEATHER_KV`, secrets, schedules and public URLs remain unchanged. Only the source repository and Workers Builds trigger moved.

Historical source remains available in this repository's Git history.
