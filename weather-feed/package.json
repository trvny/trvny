{
  "name": "weather-feed",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-source weather aggregator for Kościelec (Chrzanów) served as an Atom feed of changes. Cloudflare Worker, free tier.",
  "type": "module",
  "scripts": { "dev": "wrangler dev", "deploy": "wrangler deploy", "typecheck": "tsc --noEmit" },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.107.0"
  }
}
